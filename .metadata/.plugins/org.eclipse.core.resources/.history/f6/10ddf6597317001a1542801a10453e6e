package Negocio;
import DAO.UsuarioDAO;
import Dominio.Usuario;

public class UsuarioNegocio {

	public void VerificacionUsuario(Usuario UsuarioIngresado) {
		
		UsuarioDAO usuario = new UsuarioDAO();
		if(usuario.VerificarExistencia(UsuarioIngresado)!=null)
		
	}
}
